services:
  - type: web
    name: logos-backend
    env: node
    region: oregon
    plan: free
    rootDir: apps/backend
    buildCommand: cd ../.. && npm install && cd apps/backend && npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: POE_API_KEY
        sync: false
      - key: AGORA_APP_ID
        sync: false
      - key: AGORA_APP_CERTIFICATE
        sync: false
      - key: CORS_ORIGIN
        value: https://frontend-j7myt9g1m-kevin-owino-odhiambos-projects.vercel.app

databases:
  - name: logos-postgres
    databaseName: logos_platform
    user: logos_user
    region: oregon
    plan: free
